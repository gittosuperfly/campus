<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cai.campus.dao.UserDao">

    <resultMap type="com.cai.campus.entity.User" id="UserMap">
        <result property="uid" column="uid" jdbcType="INTEGER"/>
        <result property="realname" column="realName" jdbcType="VARCHAR"/>
        <result property="nickname" column="nickname" jdbcType="VARCHAR"/>
        <result property="phonenumber" column="phoneNumber" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select
          uid, realName, nickname, phoneNumber, password
        from campus.user
        where uid = #{uid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select
          uid, realName, nickname, phoneNumber, password
        from campus.user
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserMap">
        select
          uid, realName, nickname, phoneNumber, password
        from campus.user
        <where>
            <if test="uid != null">
                and uid = #{uid}
            </if>
            <if test="realname != null and realname != ''">
                and realName = #{realname}
            </if>
            <if test="nickname != null and nickname != ''">
                and nickname = #{nickname}
            </if>
            <if test="phonenumber != null and phonenumber != ''">
                and phoneNumber = #{phonenumber}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="uid" useGeneratedKeys="true">
        insert into campus.user(realName, nickname, phoneNumber, password)
        values (#{realname}, #{nickname}, #{phonenumber}, #{password})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update campus.user
        <set>
            <if test="realname != null and realname != ''">
                realName = #{realname},
            </if>
            <if test="nickname != null and nickname != ''">
                nickname = #{nickname},
            </if>
            <if test="phonenumber != null and phonenumber != ''">
                phoneNumber = #{phonenumber},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
        </set>
        where uid = #{uid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from campus.user where uid = #{uid}
    </delete>

</mapper>